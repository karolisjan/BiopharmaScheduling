FROM biopharma-scheduling/base

# Copy the codebase
COPY . /BiopharmaScheduling 

# Setup and install biopharma-scheduling
RUN python /BiopharmaScheduling/setup.py && pip install /BiopharmaScheduling/dist/*linux*whl

# Install additional libs for the examples
RUN pip install -r requirements.txt

EXPOSE 8888